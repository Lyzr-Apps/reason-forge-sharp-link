'use client'

// This file is a stub - 3D components have been removed
// Keeping this file to prevent build errors from cached imports

export function Background3DScene() {
  return <div className="w-full h-full bg-transparent" />
}

export function Loading3DScene() {
  return <div className="w-full h-full bg-transparent" />
}
